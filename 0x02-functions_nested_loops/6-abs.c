#include "main.h"
/**
 * _abs - computes absolute value
 * @n: integer
 * Return: Returns absolute value
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	} else if (n < 0)
		return (-n);
	else
		return (n);
}
